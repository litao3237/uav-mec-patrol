name: core-baseline-light-load

on:
  push:
    branches: [develop]
    paths:
      - '.github/workflows/core_baseline_light_load.yml'

jobs:
  compare:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      max-parallel: 9
      matrix:
        scenario: [45, 46, 47]
        algorithm: [100, 101, 102]
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          python-version: '3.13'
      - name: Install dependencies
        run: uv sync --dev
      - name: Run K50 core baseline comparison
        run: >-
          uv run python experiments/run_core_baseline_comparison.py
          --tasks 50
          --mecs 2
          --scenario-seeds ${{ matrix.scenario }}
          --algorithm-seeds ${{ matrix.algorithm }}
          --iterations 100
      - name: Upload seed-pair result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: core-light-S${{ matrix.scenario }}-A${{ matrix.algorithm }}
          path: outputs/results/baseline_core_K50_E2_S${{ matrix.scenario }}_A${{ matrix.algorithm }}_I100.json
          if-no-files-found: warn

  aggregate:
    runs-on: ubuntu-latest
    needs: compare
    if: always()
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          pattern: core-light-S*-A*
          path: outputs/results/matrix
          merge-multiple: true
      - name: Aggregate K50 results
        run: >-
          python experiments/aggregate_core_baselines.py
          --input-dir outputs/results/matrix
          --output outputs/results/core_baseline_K50_E2_S45-46-47_A100-101-102_I100_matrix.json
      - name: Upload combined result
        uses: actions/upload-artifact@v4
        with:
          name: core-baseline-K50-E2-matrix
          path: outputs/results/core_baseline_K50_E2_S45-46-47_A100-101-102_I100_matrix.json
